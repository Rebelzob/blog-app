<nav class="flex justify-between p-5 bg-cyan-800">
  <ul class="flex space-x-4">
    <li><a href="/" class="text-white text-xl border-l hover:text-blue-200">Home</a></li>
  </ul>
  <ul class="flex space-x-4">
    <% if user_signed_in? %>
      <%= form_with(url: destroy_user_session_path, method: :delete, local: false) do |form| %>
        <button type="submit" class="btn btn-outline btn-error">
          Logout
        </button>
      <% end %>
    <% else %>
      <a class="btn btn-outline btn-success" href="<%= new_user_registration_path %>">
        Sign Up
      </a>
      <a class="btn btn-outline btn-success" href="<%= new_user_session_path %>">
        Login
      </a>
    <% end %>
  </ul>
</nav>